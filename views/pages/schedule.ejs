<div id="schedule" class="mt-5">
    <% if(score.simalateEmptySchedule === "true"){ %>
        <p class="section-subtitle text-center">
            Programul va fi afișat după tragerea la sorț
        </p>
      <% } else{ %>
        <p class="section-subtitle text-center">
          Având în vedere că unele meciuri pot avea două sau trei seturi, <span style="color: #61D2B4;">programul este estimativ!</span>
        </p>
        <% displayScore=false %>
        <%- include('ranking'); -%>
        <%- include('fixtures'); -%>
     <% } %>
</div>
